# Build and deploy Quarto website to GitHub Pages
# Uses r-lib/actions (Native R) - NOT Nix - per CI skill Decision Matrix

name: Quarto Publish

on:
  push:
    branches: [main]
    paths:
      - 'vignettes/**'
      - '_quarto.yml'
      - 'index.qmd'
      - 'R/ccusage.R'
      - 'inst/extdata/ccusage_*.json'
      - '.github/workflows/quarto-publish.yaml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      # Set R_LIBS globally so all R processes (including Quarto's) use the same path
      R_LIBS: ${{ github.workspace }}/rlib
    steps:
      - uses: actions/checkout@v4

      - uses: r-lib/actions/setup-r@v2
        with:
          use-public-rspm: true

      - name: Create R library directory
        run: mkdir -p ${{ github.workspace }}/rlib

      - name: Install R packages
        run: |
          Rscript -e "
            # Create library dir if needed
            lib <- Sys.getenv('R_LIBS')
            dir.create(lib, showWarnings = FALSE, recursive = TRUE)

            # Show library paths
            cat('R_LIBS:', lib, '\n')
            cat('Library paths:', paste(.libPaths(), collapse = ', '), '\n')

            # Install packages to our library
            options(repos = c(RSPM = 'https://packagemanager.posit.co/cran/__linux__/jammy/latest', CRAN = 'https://cloud.r-project.org'))
            pkgs <- c('gh', 'dplyr', 'tidyr', 'ggplot2', 'lubridate',
                      'knitr', 'gert', 'fs', 'gt', 'gtExtras', 'scales',
                      'here', 'jsonlite', 'DT', 'htmltools', 'tibble', 'purrr', 'rmarkdown')
            install.packages(pkgs, lib = lib, Ncpus = 2)

            # Verify installation
            for (pkg in pkgs) {
              if (!requireNamespace(pkg, quietly = TRUE)) {
                stop(sprintf('Failed to install: %s', pkg))
              }
            }
            cat('All packages installed successfully\n')
          "

      - uses: quarto-dev/quarto-actions/setup@v2

      - name: Render Quarto site
        run: |
          # Create .Renviron so knitr R processes find packages
          echo "R_LIBS=${{ github.workspace }}/rlib" > .Renviron
          cat .Renviron

          # Show R paths for debugging (now using .Renviron)
          Rscript -e "cat('R_LIBS:', Sys.getenv('R_LIBS'), '\n')"
          Rscript -e "cat('Library paths:', paste(.libPaths(), collapse = ', '), '\n')"
          Rscript -e "cat('gh available:', requireNamespace('gh', quietly = TRUE), '\n')"

          # Render
          quarto render

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: docs

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

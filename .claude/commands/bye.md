Run the session tidy checks by executing `~/.claude/hooks/session_tidy.sh` and report the results.
Check for uncommitted config changes and offer to commit them.
Remind the user to update CURRENT_WORK.md if needed.

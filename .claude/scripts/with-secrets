#!/bin/bash
# with-secrets <cmd> [args...]
# Run a command with ~/.config/secrets.env loaded into the environment.
# For launchd jobs, daemons, and hooks that do not source shell rc files. (llm#791)
set -a
[ -r "$HOME/.config/secrets.env" ] && . "$HOME/.config/secrets.env"
set +a
exec "$@"

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Sweep stale worktrees across all repos under ~/docs_gh daily 09:00.     -->
    <!-- After the script's SOAK_END date (2026-06-02), --apply removes          -->
    <!-- merged+clean+old worktrees. Before then, treated as dry-run.            -->
    <!-- Tracks:        JohnGavin/llm#199.                                       -->
    <!-- Schedule fix:  JohnGavin/llm#586 (was weekly Sun 04:00 — Mac asleep,    -->
    <!--                job never auto-fired; moved to daily 09:00 when Mac is   -->
    <!--                reliably awake).                                          -->
    <!--                                                                          -->
    <!-- Install / reload:                                                       -->
    <!--   launchctl bootout gui/$(id -u)/com.claude.worktree-gc 2>/dev/null     -->
    <!--   cp .claude/launchd/com.claude.worktree-gc.plist                       -->
    <!--        ~/Library/LaunchAgents/                                           -->
    <!--   launchctl bootstrap gui/$(id -u)                                      -->
    <!--        ~/Library/LaunchAgents/com.claude.worktree-gc.plist              -->

    <key>Label</key>
    <string>com.claude.worktree-gc</string>

    <key>ProgramArguments</key>
    <array>
        <string>/Users/johngavin/docs_gh/llm/bin/launchd-recorders/worktree-gc</string>
    </array>

    <!-- Fix: launchd's default PATH (/usr/bin:/bin:/usr/sbin:/sbin) omits gh   -->
    <!-- (Homebrew), so squash-merge detection in worktree_gc.sh's             -->
    <!-- is_squash_merged() silently found nothing under the scheduler even    -->
    <!-- though interactive runs (full PATH) detected merges correctly. HOME   -->
    <!-- is set so gh reads ~/.config/gh for auth.                            -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
        <key>HOME</key>
        <string>/Users/johngavin</string>
    </dict>

    <!-- Daily 09:06 (Mac reliably awake). Staggered 6 min past :00 to avoid
         clock-minute contention with other 09:00 jobs (llm#1154). -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>9</integer>
        <key>Minute</key>
        <integer>6</integer>
    </dict>

    <key>StandardOutPath</key>
    <string>/Users/johngavin/.claude/logs/worktree_gc.out</string>

    <key>StandardErrorPath</key>
    <string>/Users/johngavin/.claude/logs/worktree_gc.err</string>

    <!-- Tolerate up to 30 s between retries if a minute is missed -->
    <key>ThrottleInterval</key>
    <integer>30</integer>

    <!-- Do NOT run on load — only on the schedule. -->
    <key>RunAtLoad</key>
    <false/>

</dict>
</plist>

<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!-- com.claude.roborev-daily-email.plist
     Daily roborev snapshot → llmtelemetry data branch → digest email.
     Runs at 08:03 local time, after the 02:00 roborev-metrics-etl job (#226).
     (Staggered 3 min past :00 to avoid clock-minute contention — llm#1154.)

     Install:
       cp .claude/launchd/com.claude.roborev-daily-email.plist \
          ~/Library/LaunchAgents/com.claude.roborev-daily-email.plist
       launchctl load -w ~/Library/LaunchAgents/com.claude.roborev-daily-email.plist

     Uninstall:
       launchctl unload ~/Library/LaunchAgents/com.claude.roborev-daily-email.plist
       rm ~/Library/LaunchAgents/com.claude.roborev-daily-email.plist

     Manual dry-run:
       DRYRUN=1 EMAIL_DRY_RUN=1 bash ~/docs_gh/llm/bin/roborev_daily_cron.sh

     Credentials: ~/.claude/env/roborev_email.env (not committed; create manually)
       GMAIL_USERNAME=you@gmail.com
       GMAIL_APP_PASSWORD=<16-char app password>
       REPORT_RECIPIENT=you@gmail.com

     ROBOREV_DASHBOARD_URL is an OPTIONAL override, not a required field --
     leave it unset unless you have a real http(s) URL to point at. As of
     2026-09-24 (llm#1123), dashboard_cta_block() in email_styles.R only
     renders a clickable button when this var is set to an http(s) URL --
     with it unset, the email shows the local rendered-vignette path as
     plain text (with a staleness note) instead of a button, because
     neither a file:// href (stripped by mail clients) nor the llmtelemetry
     repo URL (private, no GitHub Pages, since 2026-08-22) ever worked as a
     clickable target.

     Tracked in llm#287.
-->
<plist version="1.0">
<dict>
    <key>Label</key>
    <string>com.claude.roborev-daily-email</string>

    <key>ProgramArguments</key>
    <array>
        <string>/Users/johngavin/docs_gh/llm/bin/launchd-recorders/roborev-daily-email</string>
    </array>

    <!-- Daily at 08:03 local time (after 02:00 metrics-etl job).
         Staggered from :00 to avoid clock-minute contention with
         launchd-health-weekly and self-review-verify (llm#1154). -->
    <key>StartCalendarInterval</key>
    <dict>
        <key>Hour</key>
        <integer>8</integer>
        <key>Minute</key>
        <integer>3</integer>
    </dict>

    <!-- Do NOT run immediately on launchctl load -->
    <key>RunAtLoad</key>
    <false/>

    <!-- PATH must include ~/.local/bin (roborev shim — #386), nix stable profile
         (for nix-shell), and /usr/local/bin (real roborev binary).
         ~/.local/bin/ MUST precede /usr/local/bin/ so the Phase-1.6 shim fires. -->
    <key>EnvironmentVariables</key>
    <dict>
        <key>PATH</key>
        <string>/Users/johngavin/.local/bin:/nix/var/nix/profiles/default/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:/Users/johngavin/.nix-profile/bin</string>
        <key>HOME</key>
        <string>/Users/johngavin</string>
        <!-- NIX_SSL_CERT_FILE is required for nix-shell to make network calls -->
        <key>NIX_SSL_CERT_FILE</key>
        <string>/nix/var/nix/profiles/default/etc/ssl/certs/ca-bundle.crt</string>
    </dict>

    <key>StandardOutPath</key>
    <string>/Users/johngavin/.claude/logs/roborev_daily_email_launchd.log</string>

    <key>StandardErrorPath</key>
    <string>/Users/johngavin/.claude/logs/roborev_daily_email_launchd.log</string>

    <!-- Retry once after 60 s if job exits non-zero -->
    <key>ThrottleInterval</key>
    <integer>60</integer>

    <key>LowPriorityIO</key>
    <true/>
</dict>
</plist>
